#include "main.h"
/**
 * _abs - computes the absolute value of a number
 * @x: takes in an integer value as an argument
 * Return: absolute value
 */

int _abs(int x)
{
	int x, y = -1;

	if (x < 0)
	{
		return (x * y);
	}
	else
	{
		return (x);
	}
}
