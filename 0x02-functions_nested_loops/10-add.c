#include "main.h"
/**
 * add - adds two numbers together
 * @x: argument is of integer type
 * @y: argument is of integer type
 * Return: Returns sum as the result
 */

int add(int x, int y)
{
	return (x + y);
}
