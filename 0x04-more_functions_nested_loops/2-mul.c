#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: first argument of type integer that is passed to the function
 * @b: second argument of type integer that is passed to the function
 * Return: product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
